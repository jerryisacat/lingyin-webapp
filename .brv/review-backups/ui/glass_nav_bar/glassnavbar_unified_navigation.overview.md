- Key points: Replaces Sidebar.tsx + MobileTabBar.tsx with single GlassNavBar.tsx per Issue #101; desktop uses fixed 64px top frosted-glass nav with user dropdown; mobile uses bottom tab bar with 800ms long-press on settings for logout; scroll-driven opacity + framer-motion layoutId active indicators; AppShell drops md:pl-56 offset and adopts max-w-3xl mx-auto container; navConfig-driven filtering for auth/desktop/mobile items
- Structure / sections summary: Document sections include Reason, Raw Concept (task/changes/files/flow), Narrative (Structure, Dependencies, Highlights, Rules); flow described as navConfig → filter → render desktop/mobile → handle scroll/auth/logout
- Notable entities/patterns/decisions: Entities: GlassNavBar.tsx, AppShell.tsx, config/navigation.json, next-auth/react, framer-motion, lucide-react; patterns: frosted glass (bg-warm-white/80 + backdrop-blur), unified responsive nav, cache-clearing logout; decisions: mobileItems subset from config, authenticated-or-always rule for desktop items, removal of mini header